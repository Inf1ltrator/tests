<div class="col-md-6" style="align-right;">
	<div class="well" style="background-color: white">
		<ul class="list-group">
			<h4 style="margin-top: 1em;">Название:</h4>
			<li class="list-group-item" style="margin-top: 1em;"><%= @answer.name %></li>
			<h4 style="margin-top: 1em;">Описание:</h4>
			<li class="list-group-item" style="margin-top: 1em;"><%= @answer.description %></li>
		</ul>
		<% if user_signed_in? && current_user.id == @blank.uid %>
			<div class="btn-group btn-group-justified" role="group" aria-label="..." style="margin-top: 1em;">
				<%=link_to "Редактировать ответ", edit_blank_answer_path(@blank,@answer), :class => "btn btn-warning" %>
			
				<%=link_to "Удалить", blank_answer_path(@blank,@answer), :class => "btn btn-danger", method: :delete, data: {confirm: "Точно удалить?"} %>
			</div>
		<% end %>
	</div>
</div>